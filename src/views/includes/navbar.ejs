<nav class="navbar navbar-expand-lg bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand text-bg-dark" href="/">Agenda</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
            <ul class="navbar-nav p-1">
                
                <li class="nav-item">
                    <a class="nav-link text-bg-dark " href="/contato">Cadastrar contato</a>
                </li>

                <% if(user) { %>
                    <li class="nav-item">
                        <a class="nav-link text-bg-dark " href="/login/logout">Sair</a>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link text-bg-dark " href="login/">Entrar</a>
                    </li>
                <% } %>

            </ul>
        </div>
    </div>
</nav>